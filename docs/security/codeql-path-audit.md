# CodeQL path-safety audit

**Date:** 2026-08-08

**Status:** Pending final CodeQL scan and individual GitHub dispositions

This record accounts for the 65 `rust/path-injection` and `py/path-injection`
alerts opened by CodeQL default setup for main commit
`7366106f4af752c5a9df9f18a8e10eec245b9898`. It does not change CodeQL query
configuration, threat model, or alert severity.

| Alert | Rule | Source and sink | Trust boundary | Verified controls | Decision | Final state |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | `py/path-injection` | `parse_args().version` in `scripts/package_release.py` flows through `safe_component` into `archive_root = staging_root / f"skilltape-v{version}-{target}"` in `stage_release`. | Trusted local release maintainer input to an offline packaging script. | `safe_component`, `require_directory`, `require_file`, and `copy_ui` in `scripts/package_release.py`; symlink fixture coverage in `scripts/test_release_package.py`. | Trusted local release-tool input; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 2 | `py/path-injection` | `parse_args().target` in `scripts/package_release.py` flows through `safe_component` into `archive_root = staging_root / f"skilltape-v{version}-{target}"` in `stage_release`. | Trusted local release maintainer input to an offline packaging script. | `safe_component`, `require_directory`, `require_file`, and `copy_ui` in `scripts/package_release.py`; symlink fixture coverage in `scripts/test_release_package.py`. | Trusted local release-tool input; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 3 | `py/path-injection` | `parse_args().binary_dir` in `scripts/package_release.py` feeds `stage_release(..., binary_dir, ...)`, then joins `binary_dir / f"skilltape{suffix}"` and `binary_dir / f"skilltape-console-api{suffix}"`. | Trusted local release maintainer input to an offline packaging script. | `safe_component`, `require_directory`, `require_file`, and `copy_ui` in `scripts/package_release.py`; symlink fixture coverage in `scripts/test_release_package.py`. | Trusted local release-tool input; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 4 | `py/path-injection` | `binary_dir / f"skilltape{suffix}"` in `scripts/package_release.py` is copied with `shutil.copy2` into the staged archive root. | Trusted local release maintainer input to an offline packaging script. | `safe_component`, `require_directory`, `require_file`, and `copy_ui` in `scripts/package_release.py`; symlink fixture coverage in `scripts/test_release_package.py`. | Trusted local release-tool input; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 5 | `py/path-injection` | `binary_dir / f"skilltape-console-api{suffix}"` in `scripts/package_release.py` is copied with `shutil.copy2` into the staged archive root. | Trusted local release maintainer input to an offline packaging script. | `safe_component`, `require_directory`, `require_file`, and `copy_ui` in `scripts/package_release.py`; symlink fixture coverage in `scripts/test_release_package.py`. | Trusted local release-tool input; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 6 | `py/path-injection` | `parse_args().ui_dist` in `scripts/package_release.py` feeds `copy_ui(ui_dist, console_root)` and the `ui_dist.rglob("*")` copy walk. | Trusted local release maintainer input to an offline packaging script. | `safe_component`, `require_directory`, `require_file`, and `copy_ui` in `scripts/package_release.py`; symlink fixture coverage in `scripts/test_release_package.py`. | Trusted local release-tool input; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 7 | `py/path-injection` | `parse_args().output_dir` in `scripts/package_release.py` feeds `output = output_dir / f"skilltape-v{version}-{target}{suffix}"` and `partial.replace(output)`. | Trusted local release maintainer input to an offline packaging script. | `safe_component`, `require_directory`, `require_file`, and `copy_ui` in `scripts/package_release.py`; symlink fixture coverage in `scripts/test_release_package.py`. | Trusted local release-tool input; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 8 | `rust/path-injection` | Console route identifiers and storage entry names in `apps/skilltape-console-api/src/read_model.rs` feed `storage_child("tapes")`, `directory_names`, and `count_directories` under `root/.skilltape/tapes`. | Local Console HTTP requests are bounded to a canonical workspace root and a fixed storage subtree. | `ConsoleReadModel::new`, `storage_child`, `ensure_safe_path`, `validate_id`, `directory_names`, and `count_files` in `apps/skilltape-console-api/src/read_model.rs`; Task 2 route tests. | Console canonical-root directory entry; if still reported, expected disposition `false positive`. | Pending matching scan |
| 9 | `rust/path-injection` | Console route identifiers and receipt filenames in `apps/skilltape-console-api/src/read_model.rs` feed `storage_child("receipts")` and `count_files(..., Some(".json"))` under `root/.skilltape/receipts`. | Local Console HTTP requests are bounded to a canonical workspace root and a fixed storage subtree. | `ConsoleReadModel::new`, `storage_child`, `ensure_safe_path`, `validate_id`, `directory_names`, and `count_files` in `apps/skilltape-console-api/src/read_model.rs`; Task 2 route tests. | Console canonical-root directory entry; if still reported, expected disposition `false positive`. | Pending matching scan |
| 10 | `rust/path-injection` | `CaptureConfig.workspace` in `crates/skilltape-cli/src/capture_command.rs` feeds `resolve_workspace`, which canonicalizes the directory before capture starts. | Local operator-selected Capture workspace path. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Capture path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 11 | `rust/path-injection` | `CaptureConfig.output` in `crates/skilltape-cli/src/capture_command.rs` feeds `resolve_output`, which accepts an explicit local tape root only after `..` and empty-file-name rejection. | Local operator-selected Capture output path. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Capture path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 12 | `rust/path-injection` | The default Capture sink in `crates/skilltape-cli/src/capture_command.rs` is `workspace.join(".skilltape/tapes").join(name)`. | Local operator-selected Capture workspace path. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Capture path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 13 | `rust/path-injection` | The default Capture output in `crates/skilltape-cli/src/capture_command.rs` is rechecked by `validate_default_output`, which canonicalizes the nearest existing ancestor before the tape is created. | Local operator-selected Capture workspace path. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Capture path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 14 | `rust/path-injection` | The staging tape root in `crates/skilltape-cli/src/capture_command.rs` is `std::env::temp_dir().join(format!("skilltape-capture-..."))`, then passed to `TapeStore::create(&staging_root, manifest)`. | Process-owned local temporary directory for Capture staging. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Capture path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 15 | `rust/path-injection` | The validated final tape root in `crates/skilltape-cli/src/capture_command.rs` is passed to `TapeStore::create(&output, manifest)` before merged events are appended. | Local operator-selected Capture output path. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Capture path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 16 | `rust/path-injection` | The staging tape root in `crates/skilltape-cli/src/capture_command.rs` is reopened through `TapeStore::open(&staging_root)` before timeline merge. | Process-owned local temporary directory for Capture staging. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Capture path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 17 | `rust/path-injection` | The deterministic compile tests in `crates/skilltape-compiler/tests/deterministic_compile.rs` create a test-owned package root under an owned `TempDir` and pass it into compile fixtures. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 18 | `rust/path-injection` | The deterministic compile tests in `crates/skilltape-compiler/tests/deterministic_compile.rs` write fixture inputs under a `package` child of an owned `TempDir`. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 19 | `rust/path-injection` | The deterministic compile tests in `crates/skilltape-compiler/tests/deterministic_compile.rs` compare outputs rooted in an owned `TempDir` fixture tree. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 20 | `rust/path-injection` | The deterministic compile tests in `crates/skilltape-compiler/tests/deterministic_compile.rs` keep any outside target as a sibling owned by the same `TempDir` fixture parent. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 21 | `rust/path-injection` | The template tests in `crates/skilltape-core/src/template.rs` build `root = TestDir::new(...).join("skill")` and pass it to `create_skill_template(&root, name)`. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 22 | `rust/path-injection` | The template tests in `crates/skilltape-core/src/template.rs` build `first_root` under an owned `TempDir` and pass it to `create_skill_template(&first_root, "example-skill")`. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 23 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` create an owned `TempDir` package root and pass it to `SkillPackage::load(&package.root)`. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 24 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` create required fixture files under an owned `TempDir` package root before `SkillPackage::load(&package.root)`. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 25 | `rust/path-injection` | The template tests in `crates/skilltape-core/src/template.rs` build `second_root` under an owned `TempDir` and pass it to `create_skill_template(&second_root, "example-skill")`. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 26 | `rust/path-injection` | The template tests in `crates/skilltape-core/src/template.rs` build `root = TestDir::new("existing").join("existing")`, prewrite `root.join("README.md")`, and call `create_skill_template(&root, "existing")`. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 27 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` build owned `TempDir` package fixtures before lint and load checks. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 28 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` build owned `TempDir` package fixtures before symlink and missing-file checks. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 29 | `rust/path-injection` | The template tests in `crates/skilltape-core/src/template.rs` call `create_skill_template`, which joins the test-owned root with each required relative file in `TEMPLATE_FILES`. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 30 | `rust/path-injection` | The template tests in `crates/skilltape-core/src/template.rs` call `write_new_file(root.join(file), ...)` only on a test-owned root under `TempDir`. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 31 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` create owned `TempDir` fixture roots before `SkillPackage::load(&package.root)` reads the package tree. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 32 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` create owned `TempDir` fixture roots before required-file validation reads the package tree. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 33 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` create owned `TempDir` fixture roots before lockfile and permission checks read the package tree. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 34 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` keep test package files under an owned `TempDir` package child before lint reads them. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 35 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` build an owned `TempDir` package root before workflow-file checks read that tree. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 36 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` build an owned `TempDir` package root before README and SKILL fixture checks read that tree. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 37 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` build an owned `TempDir` package root before duplicate-file and missing-file fixture checks. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 38 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` build an owned `TempDir` package root before file enumeration and load checks. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 39 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` build an owned `TempDir` package root before path-normalization fixture checks. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 40 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` build an owned `TempDir` package root before package lint checks resolve fixture files. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 41 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` keep the escaping-symlink outside target as a sibling owned by the same `TempDir` parent. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 42 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` create required-file symlink fixtures under an owned `TempDir` package root. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 43 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` create package fixture paths under an owned `TempDir` before validation rejects escapes. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 44 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` create package fixture paths under an owned `TempDir` before validation rejects symlinked required files. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 45 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` create package fixture paths under an owned `TempDir` before validation rejects invalid package roots. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 46 | `rust/path-injection` | The package validation tests in `crates/skilltape-core/tests/package_validation.rs` create package fixture paths under an owned `TempDir` before validation rejects outside references. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 47 | `rust/path-injection` | Package-declared relative paths in `crates/skilltape-runner/src/workspace.rs` flow through `resolve_under(&self.root, relative)` before Replay reads or writes inside the temp workspace. | Package-derived Replay path after package validation, not a host-root path. | `validate_relative_path`, `resolve_under`, and `ensure_no_symlink_ancestors` in `crates/skilltape-runner/src/workspace.rs`; package validation and replay symbolic-link tests. | Package-derived Replay path following validation; if still reported, expected disposition `false positive`. | Pending matching scan |
| 48 | `rust/path-injection` | Package-declared output paths in `crates/skilltape-runner/src/workspace.rs` flow into `materialize_outputs(output_root, paths)` and the staging copy tree under Replay output. | Package-derived Replay path after package validation, not a host-root path. | `validate_relative_path`, `resolve_under`, and `ensure_no_symlink_ancestors` in `crates/skilltape-runner/src/workspace.rs`; package validation and replay symbolic-link tests. | Package-derived Replay path following validation; if still reported, expected disposition `false positive`. | Pending matching scan |
| 49 | `rust/path-injection` | The test-only fake adapter in `crates/skilltape-runner/tests/runner.rs` reads `request.cwd.join(relative)` only for the static names `inputs/fixture.txt` and `scripts/emit.sh`. | Test-only fake adapter, not shipped runtime code. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Test-only fake adapter using static relative names `inputs/fixture.txt` and `scripts/emit.sh`; expected disposition `used in tests`. | Pending matching scan |
| 50 | `rust/path-injection` | Package-declared script paths in `crates/skilltape-runner/src/workspace.rs` flow into `copy_referenced_scripts(package, &root)` before Replay copies scripts into the temp workspace. | Package-derived Replay path after package validation, not a host-root path. | `validate_relative_path`, `resolve_under`, and `ensure_no_symlink_ancestors` in `crates/skilltape-runner/src/workspace.rs`; package validation and replay symbolic-link tests. | Package-derived Replay path following validation; if still reported, expected disposition `false positive`. | Pending matching scan |
| 51 | `rust/path-injection` | Package-derived directory paths in `crates/skilltape-runner/src/workspace.rs` flow into `copy_entry(&source, &destination)` when Replay copies directories. | Package-derived Replay path after package validation, not a host-root path. | `validate_relative_path`, `resolve_under`, and `ensure_no_symlink_ancestors` in `crates/skilltape-runner/src/workspace.rs`; package validation and replay symbolic-link tests. | Package-derived Replay path following validation; if still reported, expected disposition `false positive`. | Pending matching scan |
| 52 | `rust/path-injection` | Package-derived file paths in `crates/skilltape-runner/src/workspace.rs` flow into `copy_entry(&source, &destination)` when Replay copies files and preserves permissions. | Package-derived Replay path after package validation, not a host-root path. | `validate_relative_path`, `resolve_under`, and `ensure_no_symlink_ancestors` in `crates/skilltape-runner/src/workspace.rs`; package validation and replay symbolic-link tests. | Package-derived Replay path following validation; if still reported, expected disposition `false positive`. | Pending matching scan |
| 53 | `rust/path-injection` | Package-derived output paths in `crates/skilltape-runner/src/workspace.rs` flow into `move_path(from, to)` before Replay renames materialized output into place. | Package-derived Replay path after package validation, not a host-root path. | `validate_relative_path`, `resolve_under`, and `ensure_no_symlink_ancestors` in `crates/skilltape-runner/src/workspace.rs`; package validation and replay symbolic-link tests. | Package-derived Replay path following validation; if still reported, expected disposition `false positive`. | Pending matching scan |
| 54 | `rust/path-injection` | Package-derived directory paths in `crates/skilltape-runner/src/workspace.rs` flow into `make_directory(path)` before Replay creates output directories. | Package-derived Replay path after package validation, not a host-root path. | `validate_relative_path`, `resolve_under`, and `ensure_no_symlink_ancestors` in `crates/skilltape-runner/src/workspace.rs`; package validation and replay symbolic-link tests. | Package-derived Replay path following validation; if still reported, expected disposition `false positive`. | Pending matching scan |
| 55 | `rust/path-injection` | Explicit local tape roots in `crates/skilltape-tape/src/store.rs` feed `validate_root(root)` before `TapeStore::create` accepts them. | Explicit local Capture or Tape path chosen by the operator or local caller. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Tape path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 56 | `rust/path-injection` | Explicit local tape roots in `crates/skilltape-tape/src/store.rs` feed `fs::create_dir_all(parent)` and `fs::create_dir(&root)` inside `TapeStore::create`. | Explicit local Capture or Tape path chosen by the operator or local caller. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Tape path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 57 | `rust/path-injection` | Explicit local tape roots in `crates/skilltape-tape/src/store.rs` feed `initialize_tape(root, manifest)`, which creates `events.jsonl` and writes `manifest.json` under that root. | Explicit local Capture or Tape path chosen by the operator or local caller. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Tape path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 58 | `rust/path-injection` | Explicit local tape roots in `crates/skilltape-tape/src/store.rs` feed `TapeStore::open(root)`, `store.read_manifest()`, and `store.root.join("events.jsonl")`. | Explicit local Capture or Tape path chosen by the operator or local caller. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Tape path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 59 | `rust/path-injection` | Explicit local tape roots in `crates/skilltape-tape/src/store.rs` feed `OpenOptions::append(true).open(self.root.join("events.jsonl"))` in `TapeStore::append`. | Explicit local Capture or Tape path chosen by the operator or local caller. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Tape path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 60 | `rust/path-injection` | `CaptureOptions.workspace` in `crates/skilltape-capture/src/session.rs` is serialized into `SessionStarted.cwd`, `TerminalCommand.cwd`, and the `PtyRequest.workspace` field. | Canonicalized local Capture workspace metadata, not an arbitrary filesystem target. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Canonicalized local Capture workspace metadata; if still reported, expected disposition `false positive`. | Pending matching scan |
| 61 | `rust/path-injection` | The Console CLI in `crates/skilltape-cli/src/console_command.rs` accepts explicit local `SKILLTAPE_CONSOLE_API_BIN`, `SKILLTAPE_CONSOLE_UI_DIST`, and workspace paths before `spawn_api` uses them. | Explicit local Console executable and UI override metadata, chosen by the operator. | `validate_workspace`, `validate_ui_dist`, and local binary existence checks in `crates/skilltape-cli/src/console_command.rs`; Task 2 route tests for the canonical-root Console model. | Explicit local Console executable override metadata; if still reported, expected disposition `false positive`. | Pending matching scan |
| 62 | `rust/path-injection` | The validated final Capture output in `crates/skilltape-cli/src/capture_command.rs` is also used in cleanup paths such as `std::fs::remove_dir_all(&output)` after a failed append. | Local operator-selected Capture output path. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Capture path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 63 | `rust/path-injection` | The template tests in `crates/skilltape-core/src/template.rs` keep every `root.join(file)` target under an owned `TempDir` package root before `OpenOptions::create_new(true)` writes the file. | Test-only fixture path, not a shipped interface. | The `TempDir` ownership change in Task 1 and the test target path for alert 49. | Task 1 removed the environment-derived fixture source; expected `fixed by scan`, otherwise only `used in tests`. | Pending matching scan |
| 64 | `rust/path-injection` | Explicit local tape roots in `crates/skilltape-tape/src/store.rs` feed `OpenOptions::read(true).write(true).create(true).open(self.root.join(".store.lock"))` in `lock_exclusive`. | Explicit local Capture or Tape path chosen by the operator or local caller. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Tape path; if still reported, expected disposition `won't fix`. | Pending matching scan |
| 65 | `rust/path-injection` | Explicit local tape roots in `crates/skilltape-tape/src/store.rs` feed `write_manifest(root, manifest)`, which uses `root.join("manifest.json.tmp")` and `fs::rename` to `root.join("manifest.json")`. | Explicit local Capture or Tape path chosen by the operator or local caller. | `resolve_workspace`, `resolve_output`, and `validate_default_output` in `crates/skilltape-cli/src/capture_command.rs`; `TapeStore::create` and `TapeStore::open` in `crates/skilltape-tape/src/store.rs`; Capture output and symlink tests. | Explicit local Tape path; if still reported, expected disposition `won't fix`. | Pending matching scan |

## Reproduce the open-alert inventory

```bash
gh api 'repos/Chumaniac/skilltape/code-scanning/alerts?state=open&per_page=100' \
  --jq '.[] | [.number, .rule.id, .most_recent_instance.location.path, (.most_recent_instance.location.start_line | tostring)] | @tsv'
```

## Non-goals and disposition rules

- This audit must not use global dismissals, `@codeql` comments, CodeQL configuration exclusions, or a threat-model downgrade.
- A CodeQL alert without sufficient source-to-sink evidence remains open.
